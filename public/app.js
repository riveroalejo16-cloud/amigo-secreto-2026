const $=s=>document.querySelector(s);const msg=t=>$('#msg').textContent=t;
$('#login').addEventListener('submit',async e=>{e.preventDefault();msg('');try{const r=await fetch('/api/login',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({username:$('#user').value.trim(),password:$('#pass').value})});const d=await r.json().catch(()=>({}));if(!r.ok){msg(d.detail?`${d.error}: ${d.detail}`:(d.error||'No fue posible ingresar.'));return}location.href=d.role==='admin'?'/admin.html':'/participante.html'}catch(err){msg('No fue posible conectar con el servidor.');}});
